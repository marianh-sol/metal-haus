services:
  - type: web
    name: metal-haus-static
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
